# ⚠ This file is no longer the primary compose file.
# The compose file for the full stack (backend + frontend + db) lives at the repo root:
#
#   docker compose up -d          (run from repo root)
#
# This file is kept only as a reference; do not run it from here.
